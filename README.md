Chạy python pipeline.py để chạy local, output sẽ đc lưu vào "Output", NHỚ CHỈNH SỬA .ENV cho hợp lý trước khi chạy.

chưa thử với docker, hiện tại build docker thì không có j xảy ra.

dùng llama3 máy gtx 1650 lúc chạy đc lúc không :(

file .html để tham khảo.

